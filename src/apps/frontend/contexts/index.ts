import { DepsProvider, useDeps } from './deps.provider';
import { AuthDetailsProvider, useAuthDetails } from './auth.provider';
import { TasksDetailsProvider, useTasks } from './tasks.provider';

export {
  DepsProvider,
  useDeps,
  AuthDetailsProvider,
  useAuthDetails,
  TasksDetailsProvider,
  useTasks,
};
